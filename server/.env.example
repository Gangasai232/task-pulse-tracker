# Server Port Configuration
PORT=5000

# JSON Web Token Secret Key
JWT_SECRET=secret-jwt-key-task-tracker-2026

# MongoDB Connection String (Atlas Cloud URI or Local MongoDB)
# If left blank, the server automatically initializes an in-memory MongoDB fallback out-of-the-box!
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/task_pulse_tracker?retryWrites=true&w=majority
